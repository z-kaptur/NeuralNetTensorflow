# Neural network structures
Implementation of different neural network structures (including convolutional, max-pooling and fully connected layers) for image classification.
Structures are tested with MNIST and SVHN (StreetView House Numbers) datasets.
Using TensorFlow library.
